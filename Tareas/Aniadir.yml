info:
  name: Aniadir
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/addTarea
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
